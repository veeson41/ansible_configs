---
#all:
#  vars_files:
#    - vars/initial.yml
#    - vars/secret
Local:
  hosts:
    localhost:
Servers:
  hosts:
    data-server:
    streaming-server:
    management-server:
    document-server:
    utilities-server:
Workstations:
  hosts:
    development-workstation:
Home-lab:
  children:
    Servers:
    Workstations: 
